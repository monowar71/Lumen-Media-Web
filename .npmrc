# typescript is aliased to @typescript/typescript6 for eslint; tsc 7 is
# @typescript/native. openapi-typescript still peers on ^5.x — accept that until
# the ecosystem catches up (see TypeScript 7 side-by-side guidance).
legacy-peer-deps=true
